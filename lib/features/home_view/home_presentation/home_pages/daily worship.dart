import 'package:flutter/material.dart';

class DailyWorship extends StatelessWidget {
  const DailyWorship({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
